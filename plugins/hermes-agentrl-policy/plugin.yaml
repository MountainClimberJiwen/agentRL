name: agentrl-policy
version: 0.1.0
description: |
  AgentRL Policy Network Plugin — injects learned behavioral strategies
  into each turn via the pre_llm_call hook.  No core code changes required.
author: agentrl-contributors
hooks:
  - pre_llm_call
categories:
  - agentrl
  - rl
  - policy
